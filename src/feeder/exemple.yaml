Pick:
  sequence:
    - ?A hasHandMovingToward ?O
    - ?A hasInHand ?O
    - parallel:
        - NOT ?O isInContainer ?S REQUIRED
        - not ?O isOnTopOf ?S
  description:
    - ?? isA PickAction
    - ?? isPerformedBy ?A
    - ?? isPerformedOn ?O
    - ?? isPerformedFrom ?S

Place:
  ordered
